var a, b, c: integer;

begin
    a := 101;
    b := 22;
    if a * b = 2222 then
        write("multiply success.")
    else
        write("multiply failed.", a * b);
    if a / b = 4 then
        write("division success.")这里是中文
    else
        write("division failed.", a * b);
end.


